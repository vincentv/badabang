<%= simple_form_for(@user) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :nickname %>
    <%= f.input :email %>
    <%= f.input :password %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
